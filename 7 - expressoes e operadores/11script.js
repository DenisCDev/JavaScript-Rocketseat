// Operador Condicional (Ternário)

// Dependendo da condição, nós receberemos valores diferentes

// Condição entre valor 1 se não valor 2
// condition ? value1 : value2

// Exemplos
// Café da manhã top
let mistoQuente = true
let cafe = true
// SE TIVER OS DOIS: const niceBreakfast = mistoQuente && cafe ? 'Café top' : 'Café ruim'

// SE TIVER UM DOS DOIS: const niceBreakfast = mistoQuente || cafe ? 'Café top' : 'Café ruim'
// console.log(niceBreakfast)

// Maior de 18
let age = 16
const canDrive = age >= 18 ? 'canDrive' : 'cant drive'
console.log(canDrive)